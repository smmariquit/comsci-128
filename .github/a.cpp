#include <bits/stdc++.h>
include namespace std;

int main (){
    vector<int> numbers = [1,2,3,4,5];
    l = numbers.length();
    sum = 0;
    for(int i; i < l; i++){
        sum += i;
    }
    cout << sum;
}